# Environment
.env
__pycache__/
venv/

# Data (large files -- download separately)
data/raw/
data/processed/
data/neo4j_data/
data/neo4j_logs/

# Jupyter
.ipynb_checkpoints/
notebooks/*.ipynb

# Evaluation results — JSONs are now committed as paper artifacts; exclude only
# the CSV mirrors and any bulk miniwob run subfolders.
evaluation/results/*.csv
evaluation/results/miniwob_runs/

# Proposal
# Project Proposal.pdf

# PyVis render artifacts (dropped into CWD by net.save_graph)
lib/

# Runtime demo outputs (screenshots, action logs, live_url recordings)
artifacts/

# Local credential vault
data/.vault_salt
data/credential_store.enc
